-- purge accounts where delete_requested_at older than 30 days
DELETE FROM users
WHERE delete_requested_at IS NOT NULL
  AND delete_requested_at <= NOW() - INTERVAL '30 days';

  psql -U postgres -d tuks

  